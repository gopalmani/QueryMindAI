'use client';

import { useEffect, useState } from 'react';
import { api, ApiError } from '@/lib/api';

export default function QueryLibraryContent(){const [items,setItems]=useState<Array<Record<string,unknown>>>([]);const [error,setError]=useState('');useEffect(()=>{api.history().then(r=>setItems(r.items)).catch(e=>setError(e instanceof ApiError?e.message:'Could not load history'))},[]);return <div className="h-full overflow-auto bg-slate-50 p-8"><div className="mx-auto max-w-6xl"><h1 className="text-3xl font-bold text-slate-900">Query History</h1><p className="mt-2 text-sm text-slate-600">Successful approved executions. Result row data is not stored.</p>{error&&<div className="mt-4 rounded bg-red-50 p-3 text-sm text-red-700">{error}</div>}<div className="mt-6 space-y-3">{items.length===0?<p className="rounded border border-dashed bg-white p-8 text-center text-sm text-slate-500">No approved queries yet.</p>:items.map(item=><article key={String(item.id)} className="rounded-xl border bg-white p-4 shadow-sm"><div className="flex justify-between"><p className="font-medium text-slate-900">{String(item.question)}</p><span className="text-xs text-slate-500">{String(item.row_count)} rows · {String(item.duration_ms)}ms</span></div><pre className="mt-3 overflow-auto whitespace-pre-wrap rounded bg-slate-950 p-3 text-xs text-slate-100">{String(item.sql)}</pre><p className="mt-2 text-xs text-slate-500">{new Date(String(item.created_at)).toLocaleString()}</p></article>)}</div></div></div>}
